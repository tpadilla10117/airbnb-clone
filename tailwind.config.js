//Configuration for tailwind:

module.exports = {
  mode: 'jit',
  purge: ['./pages/**/*.{js,ts,jsx,tsx}', './components/**/*.{js,ts,jsx,tsx}'],
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  //We can add additional plugins into tailwind:
  plugins: [
    require('tailwind-scrollbar-hide')
  ],
}
